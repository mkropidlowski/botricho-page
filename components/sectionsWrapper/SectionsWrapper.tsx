const SectionsWrapper = ({ children }: { children: React.ReactNode }) => {
    return <div className="max-w-[1240px] flex items-center justify-center flex-col m-auto">{children}</div>;
};

export default SectionsWrapper;
